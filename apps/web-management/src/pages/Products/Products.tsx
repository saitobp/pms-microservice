import { FC } from 'react'
import { Text, theme } from '@pms-microservices/ui'

const Products: FC = () => {
  return (
    <div style={{ padding: theme.spacing.medium, width: '100%' }}>
      <Text>Produtos</Text>
    </div>
  )
}

export default Products
